# Multiply Strings

* Multiplication of a `n1`-digit number and a `n2`-digit number
  will generate at most `n1+n2` digits.
* Index two numbers in __reverse__ order.
* `i`th in `n1` and `j`th in `n2` becomes `i+j`th in new number.
